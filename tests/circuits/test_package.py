"""Import smoke test for the circuits component package (placeholder until the circuits group lands)."""

import flowguard.circuits


def test_package_imports_with_docstring():
    assert flowguard.circuits.__doc__
